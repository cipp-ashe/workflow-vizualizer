/**
 * Types for the TestButton component
 */

/**
 * Props for the TestButton component
 * Currently, this component doesn't accept any props
 */
export type TestButtonProps = Record<string, never>;
